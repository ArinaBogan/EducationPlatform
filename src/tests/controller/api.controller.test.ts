import request from 'supertest';
import app from '../../app';

test('/api/reg', async () => {
//   const res = await request(app).post('/api/reg').send({name: 'Test1',surname: 'Test1',email: 'Test1@gmail.com',pwd: '123456789',});
//   expect(res.statusCode).toBe(200);
//   expect(res.body).toBeTruthy();
//   expect(res.body.length).toBeGreaterThanOrEqual(0);
});

test('/api/auth', async () => {
//   const res = await request(app).post('/api/auth').send({email: 'Test1111@gmail.com',pwd: '123456789',});
//   expect(res.statusCode).toBe(200);
//   expect(res.body).toBeTruthy();
//   expect(res.body.length).toBeGreaterThanOrEqual(0);
});